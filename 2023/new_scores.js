"use strict";

//TODO: Add a more plots
//TODO: Sort division record by score, div record and total pts
//TODO: fix bar chart label below X axis

// Create an array of teamNames  (see scores.js)
// Create map with team and array each weeks opponent:  key fat, array ice, roll, etc...
// Create map with team and each weeks score (name based on the same name as in the map (eg roll))

var porScores = [ 0.0 ];  // Porkchops (Noah)
var cocScores = [ 0.0 ];  // Coco Browns (Andy)
var mhcScores = [ 0.0 ];  // Mile High Chubb  (Matt)
var preScores = [ 0.0 ];  // Predators (Joe)
var steScores = [ 0.0 ];  // Superbowl DB Steve's Team (Steve)

var fatScores = [ 0.0 ];  // Phat Bastards (Steve C)
var iceScores = [ 0.0 ];  // Iceotopes (John)
var belScores = [ 0.0 ];  // Discount Belicheck (Harrision)
var wbdScores = [ 0.0 ];  // Weapons of Bass Destruction (Daniel)
var rekScores = [ 0.0 ];  // Reek's Standout Team (Reek)


// 0 - not in playoffs yet
// 1 - clinched playoff spot
// 2 - clinched division
// 3 - clinched first round bye
// 4 - eliminated from playoffs

var porPlayoffs = 0;  // Porkchops (Noah)
var cocPlayoffs = 0;  // Coco Browns (Andy)
var mhcPlayoffs = 0;  // Mile High Chub  (Matt)
var prePlayoffs = 0;  // Predators (Joe)
var stePlayoffs = 0;  // Steves Superbowl DB Team (Steve)

var fatPlayoffs = 0;  // Phat Bastards (Steve)
var icePlayoffs = 0;  // Iceotopes (John)
var belPlayoffs = 0;  // Discount Belicheck (Harrision)
var wbdPlayoffs = 0;  // Weapons of Bass Destruction (Daniel)
var rekPlayoffs = 0;  // Reek's Standout Team (Reek)


// Weekly blah blah blah text...

var week0_txt = "Welcome to the 2023 Phantom League.";

var week1_txt = "";

var week2_txt = "";

var week3_txt = "";

var week4_txt = "";
 
var week5_txt = "";

var week6_txt = "";

var week7_txt = "";

var week8_txt = "";

var week9_txt = "";

var week10_txt = "";

var week11_txt = "";

var week12_txt = "";

var week13_txt = "";

var week14_txt = "";
